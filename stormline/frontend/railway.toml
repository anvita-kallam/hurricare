[build]
builder = "nixpacks"
# Railpack installs deps in a separate step — do not run npm ci here (EBUSY on node_modules/.cache).
buildCommand = "npm run build"

[deploy]
startCommand = "npm start"
healthcheckPath = "/"
restartPolicyType = "on_failure"
